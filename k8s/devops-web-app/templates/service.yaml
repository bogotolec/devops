apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-web-app.fullname" . }}
  labels:
    {{- include "devops-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-web-app.selectorLabels" . | nindent 4 }}
